# Инструкция по установке

## Шаг 1: Настройка базы данных

### Вариант А: Через phpMyAdmin

1. Откройте phpMyAdmin в браузере (обычно http://localhost/phpmyadmin)
2. Войдите под пользователем `root` (без пароля на локальном сервере)
3. Нажмите "Создать базу данных"
4. Введите имя: `pedagogy_platform`
5. Выберите кодировку: `utf8mb4_unicode_ci`
6. Нажмите "Создать"
7. Выберите созданную базу слева
8. Перейдите на вкладку "Импорт"
9. Нажмите "Выберите файл" и выберите файл `database/schema.sql`
10. Нажмите "Вперёд" внизу страницы
11. После успешного импорта схемы, импортируйте тестовые данные: `database/seed.sql`

### Вариант Б: Через Sequel Pro / TablePlus (для Mac)

1. Откройте Sequel Pro или TablePlus
2. Создайте новое подключение:
   - Host: `127.0.0.1` или `localhost`
   - Username: `root`
   - Password: (пусто для локального сервера)
   - Port: `3306`
3. Подключитесь
4. Создайте новую базу данных:
   ```sql
   CREATE DATABASE pedagogy_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   ```
5. Выберите базу `pedagogy_platform`
6. Откройте файл `database/schema.sql` и выполните его
7. Откройте файл `database/seed.sql` и выполните его

### Вариант В: Через командную строку (если MySQL установлен)

```bash
# Создать базу данных
mysql -u root -e "CREATE DATABASE pedagogy_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

# Импортировать схему
mysql -u root pedagogy_platform < database/schema.sql

# Импортировать тестовые данные
mysql -u root pedagogy_platform < database/seed.sql
```

## Шаг 2: Проверка установки

1. Откройте файл `.env` и убедитесь, что настройки базы данных правильные:
   ```
   DB_HOST=localhost
   DB_NAME=pedagogy_platform
   DB_USER=root
   DB_PASS=
   ```

2. Откройте в браузере: http://localhost/index.php

3. Если вы видите страницу с конкурсами - всё работает! ✅

## Шаг 3: Установка Composer зависимостей (для будущих фаз)

Когда будете готовы к Фазе 5 (оплата) и Фазе 7 (PDF), установите зависимости:

```bash
cd "/Users/LubsanMoloksonov1/Desktop/Педпортал каменный город"

# Установить Composer, если его нет
# Для Mac:
brew install composer

# Затем установить зависимости:
composer install
```

## Данные для входа

### Админ-панель
После импорта `seed.sql` будет создан администратор:
- Логин: `admin`
- Пароль: `admin123`

**⚠️ ВАЖНО: Смените пароль после первого входа!**

## Тестовые конкурсы

После импорта `seed.sql` у вас будут следующие конкурсы:
1. Мастерская гения (350₽)
2. Новые идеи (350₽)
3. Вдохновение (300₽)
4. Проектно-исследовательские работы учащихся (400₽)
5. Инновационные методики и технологии (350₽)
6. Педагогические идеи и технологии: дошкольное образование (350₽)

## Проверка функциональности

### Фаза 1-2 (Сейчас доступно):
- ✅ Главная страница с конкурсами
- ✅ Фильтр по категориям
- ✅ Детальная страница конкурса
- ✅ Адаптивный дизайн (откройте на телефоне)
- ✅ Фиолетовая градиентная тема

### Следующие фазы (в разработке):
- 📋 Форма регистрации (Фаза 3)
- 📋 Корзина с акцией 2+1 (Фаза 4)
- 📋 Оплата через ЮКасса (Фаза 5)
- 📋 Личный кабинет (Фаза 6)
- 📋 Генерация PDF дипломов (Фаза 7)
- 📋 Админ-панель (Фаза 8)

## Устранение проблем

### Ошибка подключения к базе данных
1. Проверьте, что MySQL запущен
2. Проверьте настройки в `.env`
3. Убедитесь, что база данных `pedagogy_platform` создана

### Страница не открывается
1. Убедитесь, что веб-сервер (Apache/Nginx) запущен
2. Проверьте, что файлы находятся в правильной директории веб-сервера
3. Для встроенного PHP сервера используйте:
   ```bash
   cd "/Users/LubsanMoloksonov1/Desktop/Педпортал каменный город"
   php -S localhost:8000
   ```

### CSS не загружается
1. Проверьте пути к файлам в браузере (F12 → Network)
2. Убедитесь, что файл `assets/css/main.css` существует

## Дальнейшие шаги

После успешной установки вы готовы к Фазе 3!
Скажите мне, когда будете готовы, и я создам форму регистрации с выбором шаблона диплома.
