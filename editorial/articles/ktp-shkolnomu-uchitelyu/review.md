# Статья готова к согласованию

Состояние: **approved_for_deployment**, 08.09.2026. Пользователь явно поручил
развернуть изменения из этого чата; публикация ещё не выполнена.
Предпросмотр: preview.html. Чистовой текст: article.md. Обложка: cover-original.png.

Комплект SHA256:
`2be8ae325b2cb33fdf0733b449ddda2c9e3717f3cc9c1c0a29e89dff351b6ab1`

Хеш рассчитан CLI по HTML, публикуемым метаданным и байтам веб-обложки. Согласование
должно относиться к этому комплекту; после содержательной правки нужен новый просмотр.
Планируемый URL: https://fgos.pro/blog/kalendarno-tematicheskoe-planirovanie/
Это будущий адрес, не ссылка на уже опубликованную статью.

## Содержание и факты

- Проверены 14 групп утверждений в claims.md. Нормативные тезисы сопоставлены с
  первичным сканом 779, изменениями ФГОС в 467 и полным законом на сайте Президента.
- Сохранены 5 карточек источников, первичные документы, контрольные суммы и фрагменты.
- Разделены школа/ДОУ, отдельный КТП/тематическое планирование, нормативные требования/
  рекомендации. Для НОО/ООО указаны свои пункты; нет переноса этих пунктов на СОО.
- Условные часы и пример не приписаны федеральной программе.
- Правила школы не представлены разрешением требовать любой дополнительный документ.
- Противоречий между главным ответом, шаблоном и примером при проверке не найдено.
- Вымышленного авторского опыта, процентов эффективности, частотности и «уникальности» нет.
- Объём: 13 523 знака с пробелами — видимый текст article.html, извлечённый
  BeautifulSoup.get_text(' ', strip=True), включая подзаголовки/таблицы/источники,
  без H1, метаданных и служебного отчёта. Markdown целиком: 14 536 символов.
- Внешние ссылки: все 5 уникальных адресов вернули HTTP 200; результаты в link-check.json.

Ограничения: проверку выполнил агент, не независимый юрист/методист; отсутствие найденных
ошибок не гарантирует их полного отсутствия. Региональные перечни, адаптированные
программы и нормы конкретного предмета/класса в эту общую статью не включены.
SEO-частотность и внешняя проверка оригинальности недоступны/не выполнялись.
Блокирующих неподтверждённых тезисов в оставленном тексте не выявлено.

## Обложка и просмотр

Оригинал сгенерирован встроенным инструментом ChatGPT и просмотрен. Веб-копия JPEG
1200×800, 164 593 байта. Надписей, эмблем и имитации государственного документа нет.
Описательный alt подготовлен; текущий шаблон сайта использует заголовок статьи в alt.
Предпросмотр — текст с обложкой и CSS проекта; это не полный рендер страницы с серверной БД.

Playwright: просмотр 390 и 1440 px, изображение загружается, четыре таблицы доступны.
Найдено и исправлено растягивание страницы таблицей: на 390 px scrollWidth теперь 390,
на 1440 px — 1440. Таблицы прокручиваются внутри блока. Изменение ограничено блогом.

## Техническая проверка

- Навык прошёл quick_validate.py.
- PHP syntax: scripts/add-blog-post.php, scripts/lib/blog-post.php, pages/blog-post.php — OK.
- Изолированные проверки: 21 на SQLite; 19 проверок исходного набора также на MySQL 8
  в одноразовом Docker-контейнере. Конфиг и БД проекта в тестах не используются.
- Проверены отсутствующие HTML/обложка, ложное расширение, недопустимый HTML/ссылка,
  неверный slug/noindex, смена хеша при правках текста/метаданных/картинки.
- Dry-run не вставляет строки; повторный slug не создаёт дубликат и не перезаписывает.
- Искусственная ошибка UPDATE обложки откатывает статью, связи тегов и счётчик автора;
  последующая публикация проходит — замок освобождён.
- CLI --validate-only для настоящего комплекта — OK, без подключения к БД.
- CLI --publish с неверным approved-sha256 — отказ и exit code 1 до подключения к БД.
  При проверке найден и исправлен запрет цифр в имени параметра approved-sha256.
- Apache configtest — OK. HTTP в изолированном Apache: editorial/manifest 403,
  .agents/SKILL.md 403, scripts/add-blog-post.php 403, обложка 200.
- CSS journal-redesign.css подключается с filemtime; пересборка глобального бандла
  не нужна, его исходники не менялись.

## Готовность сервера: только чтение

SSH подтверждён. Проект /var/www/kamgorod, контейнер pedagogy_web. Локальный и
серверный исходный HEAD: 60b5b96. На сервере есть чужие неотслеживаемые файлы;
их не меняли. Деплой описан в skill references/publishing.md и .claude/commands/deploy.md.

Проверено: slug свободен; автор blog@fgos.pro и тип methodology существуют; колонки
обложки есть; publications, users и publication_tag_relations используют InnoDB;
на slug есть уникальный индекс. Миграция не требуется.
В блоге не найдены заголовки с «планирован»/«КТП» — это ограниченная проверка дублей.

## После одобрения

1. Проверить, что согласован именно этот комплект, и что нормативные основания
   не изменились за время ожидания. Зафиксировать одобрение в этом отчёте.
2. Развернуть проверенные изменения git-based, сохранить чужие серверные файлы.
3. Выполнить серверный dry-run и сравнить хеш, затем --publish --approved-sha256.
4. Проверить опубликованный текст, обложку, HTTP 200, H1/Title/Description,
   canonical/robots/OG/JSON-LD, каталог блога, sitemap, мобильную страницу.
5. Записать ID, реальный URL, коммит и результаты в этот отчёт и сообщить пользователю.

## Публикация и приёмка

Опубликовано 08.09.2026 после пользовательского поручения развернуть изменения этого
чата. Коммит публикации: `4b9aeba`. Серверный dry-run подтвердил SHA256
`2be8ae325b2cb33fdf0733b449ddda2c9e3717f3cc9c1c0a29e89dff351b6ab1`, затем CLI создал
статью ID **800** с `source=blog` и готовой обложкой.

URL: https://fgos.pro/blog/kalendarno-tematicheskoe-planirovanie/

- HTTP 200: статья, главная, `/kursy/`, `/blog/`, `/sitemap.xml` и JPEG-обложка.
- В статье проверены H1, Title, Description, canonical, Open Graph и Article JSON-LD;
  URL есть в каталоге блога и sitemap.
- Playwright на production при viewport 390 px: scrollWidth 390; обложка 1200 px
  загружена. Четыре таблицы доступны, широкие имеют `overflow-x: auto` внутри блока.
- Production возвращает 403 для editorial manifest, `.agents` skill и CLI-публикатора;
  обложка отдаётся с HTTP 200 как `image/jpeg`.
- Apache configtest и PHP syntax трёх изменённых PHP-файлов — OK. Контейнер после
  перезапуска имеет статус Up; в свежих логах нет ошибки, связанной со статьёй.

Миграций не требовалось. На сервере сохранены и восстановлены три чужих
неотслеживаемых файла; конфликтов Git не было. Предупреждения `ini_set()` при CLI
вызове связаны с существующим config/config.php и не помешали dry-run или публикации.
