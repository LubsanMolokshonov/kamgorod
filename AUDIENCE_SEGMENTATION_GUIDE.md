# 🎯 Руководство по системе сегментации аудитории

## Обзор системы

Система сегментации аудитории позволяет организовать конкурсы по двум уровням:
1. **Типы учреждений** (ДОУ, Начальная школа, Средняя/старшая школа, СПО)
2. **Специализации** (Воспитатель, Учитель математики, Учитель физики и т.д.)

Один конкурс может быть привязан к нескольким типам учреждений и специализациям одновременно.

---

## 🚀 Быстрый старт

### 1. Миграция базы данных

Если миграции еще не выполнены, выполните SQL файлы:

```bash
# Подключитесь к MySQL
mysql -u root -p pedagogy_portal

# Выполните миграции
source database/migrations/002_add_audience_segmentation.sql
source database/migrations/002_seed_audience_data.sql
```

### 2. Структура базы данных

**Новые таблицы:**
- `audience_types` - типы учреждений (ДОУ, школы и т.д.)
- `audience_specializations` - специализации (воспитатель, учитель математики и т.д.)
- `competition_audience_types` - связь конкурсов с типами (many-to-many)
- `competition_specializations` - связь конкурсов со специализациями (many-to-many)

---

## 📂 Структура файлов

### Backend классы

**classes/AudienceType.php**
- `getAll($activeOnly)` - получить все типы
- `getById($id)` - получить тип по ID
- `getBySlug($slug)` - получить тип по slug
- `getSpecializations($audienceTypeId)` - получить специализации типа
- `create($data)` - создать новый тип
- `update($id, $data)` - обновить тип
- `delete($id)` - удалить тип
- `getCompetitionCount($audienceTypeId)` - количество конкурсов

**classes/AudienceSpecialization.php**
- `getById($id)` - получить специализацию по ID
- `getBySlug($audienceTypeId, $slug)` - получить по slug
- `getByAudienceType($audienceTypeId)` - получить все по типу
- `create($data)` - создать специализацию
- `update($id, $data)` - обновить
- `delete($id)` - удалить
- `getCompetitionCount($specializationId)` - количество конкурсов

**classes/Competition.php (новые методы)**
- `setAudienceTypes($competitionId, $audienceTypeIds)` - установить типы для конкурса
- `setSpecializations($competitionId, $specializationIds)` - установить специализации
- `getAudienceTypes($competitionId)` - получить типы конкурса
- `getSpecializations($competitionId)` - получить специализации конкурса
- `getByAudienceType($audienceTypeSlug, $category)` - получить конкурсы по типу
- `getBySpecialization($specializationSlug, $category)` - получить по специализации
- `getFilteredCompetitions($filters)` - фильтрованный поиск

### Frontend страницы

**pages/audience.php**
- Посадочные страницы для каждого типа учреждения
- URL: `/dou`, `/nachalnaya-shkola`, `/srednyaya-starshaya-shkola`, `/spo`
- Фильтрация по специализациям внутри типа

**index.php**
- Карточки выбора типа аудитории
- Расширенная панель фильтров
- Dropdown меню в header

**pages/competition-detail.php**
- Теги типов учреждений (clickable)
- Теги специализаций (informational)

### Админ-панель

**admin/audience-types/index.php** - список типов
**admin/audience-types/create.php** - создание типа
**admin/audience-types/edit.php** - редактирование типа
**admin/audience-types/specializations.php** - управление специализациями
**admin/competitions/edit.php** - редактор конкурсов с сегментацией

---

## 🔧 Использование системы

### Для администраторов

#### Добавление нового типа учреждения

1. Откройте админ-панель: `/admin/audience-types/`
2. Нажмите "Добавить тип"
3. Заполните форму:
   - **Название** - полное название типа (например: "ДОУ (Дошкольное образовательное учреждение)")
   - **Slug** - URL-friendly имя (например: "dou")
   - **Описание** - краткое описание для посадочной страницы
   - **Порядок отображения** - число для сортировки
   - **Активен** - отображается ли на сайте
4. Сохраните

#### Добавление специализаций

1. Откройте тип учреждения в админ-панели
2. Нажмите "📚" или "Управление специализациями"
3. Нажмите "Добавить специализацию"
4. Заполните форму в модальном окне
5. Сохраните

#### Привязка конкурса к аудитории

1. Откройте конкурс для редактирования: `/admin/competitions/edit.php?id=X`
2. В разделе "Сегментация аудитории" выберите:
   - **Типы учреждений** - отметьте галочками нужные типы
   - **Специализации** - отметьте специализации (сгруппированы по типам)
3. Сохраните изменения

### Для пользователей

#### Навигация по типам

**Способ 1: Dropdown меню**
- Нажмите на кнопку "Для кого конкурсы?" в header
- Выберите нужный тип учреждения

**Способ 2: Карточки на главной**
- Прокрутите до секции "Выберите свою аудиторию"
- Нажмите на карточку нужного типа

**Способ 3: Прямые ссылки**
- `/dou` - для ДОУ
- `/nachalnaya-shkola` - для начальной школы
- `/srednyaya-starshaya-shkola` - для средней/старшей школы
- `/spo` - для СПО

#### Фильтрация на главной странице

1. В панели фильтров выберите:
   - **Тип аудитории** - dropdown с типами учреждений
   - **Категория** - методические, творческие, исследовательские
2. Нажмите "Применить фильтры"

#### Просмотр конкурса

На странице конкурса отображаются:
- **Типы учреждений** - clickable теги с градиентом (ведут на посадочную страницу)
- **Специализации** - информационные теги с outline стилем

---

## 🎨 Стили и компоненты

### CSS классы

**.audience-dropdown** - dropdown меню в header
**.audience-cards-grid** - сетка карточек выбора аудитории
**.audience-card** - отдельная карточка типа
**.audience-hero** - hero секция на посадочных страницах
**.specialization-tabs** - табы для фильтрации по специализациям
**.filters-panel** - панель фильтров на главной
**.audience-tag** - clickable тег типа учреждения
**.specialization-tag** - информационный тег специализации

### Цветовая схема

Все компоненты используют основной фиолетовый цвет платформы:
- `--primary-purple: #8742ee`
- `--light-purple: rgba(135, 66, 238, 0.1)`
- `--gradient-primary` - фиолетовый градиент

---

## 📊 SEO оптимизация

### URL структура

**Посадочные страницы типов:**
- `/dou` → pages/audience.php?slug=dou
- `/nachalnaya-shkola` → pages/audience.php?slug=nachalnaya-shkola

**Фильтрация по специализации:**
- `/dou?specialization=vospitatel&category=methodology`

**Реализация через .htaccess:**
```apache
RewriteRule ^(dou|nachalnaya-shkola|srednyaya-starshaya-shkola|spo)$ pages/audience.php?slug=$1 [L,QSA]
```

### Meta теги

Каждая посадочная страница имеет уникальные:
- `<title>` - название типа + " | " + SITE_NAME
- `<meta description>` - описание из базы данных
- Open Graph теги для соцсетей

---

## 🔍 Примеры использования API

### Получить все активные типы

```php
$audienceTypeObj = new AudienceType($db);
$types = $audienceTypeObj->getAll(true); // только активные
```

### Получить конкурсы для ДОУ

```php
$competitionObj = new Competition($db);
$competitions = $competitionObj->getByAudienceType('dou', 'all');
```

### Получить специализации типа

```php
$audienceTypeObj = new AudienceType($db);
$specializations = $audienceTypeObj->getSpecializations($typeId, true);
```

### Привязать конкурс к типам и специализациям

```php
$competitionObj = new Competition($db);

// Привязать к типам (массив ID)
$competitionObj->setAudienceTypes($competitionId, [1, 2, 3]);

// Привязать к специализациям (массив ID)
$competitionObj->setSpecializations($competitionId, [5, 7, 12]);
```

### Фильтрованный поиск

```php
$competitionObj = new Competition($db);
$competitions = $competitionObj->getFilteredCompetitions([
    'audience_type' => 2,           // ID типа
    'specialization' => 5,           // ID специализации
    'category' => 'methodology',     // категория
    'is_active' => true
]);
```

---

## ✅ Чек-лист внедрения

- [x] Миграции базы данных выполнены
- [x] Seed данные загружены (4 типа, 37 специализаций)
- [x] PHP классы созданы и протестированы
- [x] Frontend страницы работают
- [x] Админ-панель доступна
- [x] URL rewriting настроен
- [x] CSS стили добавлены
- [x] Header dropdown работает
- [x] Фильтрация на главной работает
- [x] Теги на страницах конкурсов отображаются
- [ ] Протестировано на всех браузерах
- [ ] Мобильная версия проверена
- [ ] Существующие конкурсы привязаны к аудиториям

---

## 🐛 Troubleshooting

### Проблема: 404 на посадочных страницах

**Решение:** Проверьте, что .htaccess правильно настроен и mod_rewrite включен:
```bash
# Проверка Apache
apache2ctl -M | grep rewrite
# или
httpd -M | grep rewrite
```

### Проблема: Dropdown меню не работает

**Решение:** Проверьте, что JavaScript в header.php загружается:
```javascript
// Должно быть в includes/header.php
document.getElementById('audienceDropdownBtn').addEventListener('click', ...);
```

### Проблема: Специализации не отображаются

**Решение:** Проверьте, что specializations связаны с правильным audience_type_id:
```sql
SELECT s.*, at.name as type_name
FROM audience_specializations s
JOIN audience_types at ON s.audience_type_id = at.id;
```

### Проблема: Конкурсы не фильтруются

**Решение:** Проверьте таблицы связей:
```sql
-- Проверить связи типов
SELECT * FROM competition_audience_types WHERE competition_id = X;

-- Проверить связи специализаций
SELECT * FROM competition_specializations WHERE competition_id = X;
```

---

## 📞 Дополнительная помощь

Если возникли проблемы:
1. Проверьте логи PHP: `/var/log/php_errors.log`
2. Проверьте логи Apache: `/var/log/apache2/error.log`
3. Включите debug режим в config/database.php

---

**Дата создания:** 2026-01-13
**Версия:** 1.0
**Статус:** ✅ Готово к использованию
